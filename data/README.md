# Local data

JSON files in this directory are created and populated by the server at runtime.
They are intentionally ignored by Git because they contain local users, sessions,
contacts, messages, and other private application data.

Start the server with `npm start` to initialize the default data files.
